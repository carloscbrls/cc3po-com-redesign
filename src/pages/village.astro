---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Your Digital Team — CC3PO" description="Meet the CC3PO Village — 26 specialized AI agents who handle compliance, content, reviews, email, research, and operations. Each with their own personality, expertise, and story.">
  <div id="village-app" role="main" aria-label="Main content">
    <!-- Canvas for particles -->
    <canvas id="particle-canvas"></canvas>

    <!-- Header -->
    <header class="village-header">
      <div class="header-content">
        <h1 class="village-title">Your Digital Team</h1>
        <p class="village-subtitle">26 specialists with their own voices, faces, and expertise — coordinated so your business never stops.</p>
        <div class="status-bar">
          <span class="status-dot active"></span>
          <span class="status-text">All Systems Operational</span>
          <span class="status-separator">|</span>
          <span class="status-count"><span id="active-count">26</span> Team Members Active</span>
        </div>
      </div>
    </header>

    <!-- How It Works Section -->
    <section class="how-it-works">
      <h2 class="section-heading">How Your Digital Team Works</h2>
      <div class="steps-grid">
        <div class="step-card">
          <span class="step-number">1</span>
          <h3>Map Your Workflow</h3>
          <p>We break down your process step by step — lead intake, follow-up, scheduling, reporting — and identify where things slow down or fall through the cracks.</p>
        </div>
        <div class="step-card">
          <span class="step-number">2</span>
          <h3>Assign the Right Assistants</h3>
          <p>Each step gets a specialist — a researcher for deep-dive, a writer for outreach, a dispatcher for routing. No generalists doing everything poorly.</p>
        </div>
        <div class="step-card">
          <span class="step-number">3</span>
          <h3>Coordinate in Real Time</h3>
          <p>Your digital assistants hand off tasks to each other automatically — like a well-run office where the researcher's findings go straight to the writer, then to the dispatcher.</p>
        </div>
        <div class="step-card">
          <span class="step-number">4</span>
          <h3>Dashboard + Human Override</h3>
          <p>See everything in one place. Approve, adjust, or take over any step. You're always in control — the team works for you, not instead of you.</p>
        </div>
      </div>
    </section>

    <!-- Main Team Section -->
    <main class="village-main">

      <!-- Command -->
      <section class="department" id="dept-command">
        <div class="department-label">
          <span class="dept-icon">🎯</span>
          <h2>Command</h2>
          <span class="dept-tagline">Strategic coordination & leadership</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Intelligence -->
      <section class="department" id="dept-intelligence">
        <div class="department-label">
          <span class="dept-icon">🔬</span>
          <h2>Intelligence</h2>
          <span class="dept-tagline">Research, analysis & insights</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Content Studio -->
      <section class="department" id="dept-content">
        <div class="department-label">
          <span class="dept-icon">🎨</span>
          <h2>Content Studio</h2>
          <span class="dept-tagline">Writing, media & publishing</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Operations -->
      <section class="department" id="dept-operations">
        <div class="department-label">
          <span class="dept-icon">⚙️</span>
          <h2>Operations</h2>
          <span class="dept-tagline">Tasks, onboarding & support</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Communications -->
      <section class="department" id="dept-communications">
        <div class="department-label">
          <span class="dept-icon">📡</span>
          <h2>Communications</h2>
          <span class="dept-tagline">Email, phone & social outreach</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Engineering -->
      <section class="department" id="dept-engineering">
        <div class="department-label">
          <span class="dept-icon">🔧</span>
          <h2>Engineering</h2>
          <span class="dept-tagline">Automation & infrastructure</span>
        </div>
        <div class="agents-grid"></div>
      </section>

      <!-- Compliance -->
      <section class="department" id="dept-compliance">
        <div class="department-label">
          <span class="dept-icon">🛡️</span>
          <h2>Compliance</h2>
          <span class="dept-tagline">Audits, accessibility & reporting</span>
        </div>
        <div class="agents-grid"></div>
      </section>

    </main>

    <!-- Workday Animation -->
    <section class="workday-section" id="workday">
      <h2 class="section-heading">Watch a Lead Flow Through the Village</h2>
      <p class="section-subtitle">This is what coordination looks like. One lead. Every specialist. Zero manual effort.</p>
      <div class="workday-container">
        <button class="workday-btn" id="workday-btn">
          <span class="workday-btn-icon">▶</span>
          Play Workday
        </button>
        <div class="workday-flow" id="workday-flow">
          <div class="workday-step" id="step-1">
            <div class="step-avatar">🤝</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">11:02 AM</div>
              <div class="step-agent">Mateo</div>
              <div class="step-role">Lead Handler</div>
              <div class="step-desc">New lead from website form. Business name: "Sunrise Dental." Added to pipeline.</div>
            </div>
          </div>
          <div class="workday-step" id="step-2">
            <div class="step-avatar">🔍</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">11:03 AM</div>
              <div class="step-agent">Apollo</div>
              <div class="step-role">Deep Research</div>
              <div class="step-desc">Researching Sunrise Dental. Found 2 competitors in area, outdated website, no social presence.</div>
            </div>
          </div>
          <div class="workday-step" id="step-3">
            <div class="step-avatar">🕸️</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">11:15 AM</div>
              <div class="step-agent">Weaver</div>
              <div class="step-role">Content Strategy</div>
              <div class="step-desc">Drafted personalized email based on Apollo's research. Highlighted 3 specific gaps.</div>
            </div>
          </div>
          <div class="workday-step" id="step-4">
            <div class="step-avatar">🦁</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">11:20 AM</div>
              <div class="step-agent">Leo</div>
              <div class="step-role">Email Outreach</div>
              <div class="step-desc">Email sent to lead. Warmed up, personal, 3 specific talking points. Awaiting response.</div>
            </div>
          </div>
          <div class="workday-step" id="step-5">
            <div class="step-avatar">🛡️</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">2:34 PM</div>
              <div class="step-agent">Sentinel</div>
              <div class="step-role">Compliance</div>
              <div class="step-desc">Ran ADA compliance scan on lead's current website. 14 violations found. Report queued for follow-up.</div>
            </div>
          </div>
          <div class="workday-step" id="step-6">
            <div class="step-avatar">✨</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">3:15 PM</div>
              <div class="step-agent">Spark</div>
              <div class="step-role">Social Engagement</div>
              <div class="step-desc">Lead responded to email! Scheduled a call for tomorrow. Social profiles added to CRM.</div>
            </div>
          </div>
          <div class="workday-step" id="step-7">
            <div class="step-avatar">🌍</div>
            <div class="step-connector"></div>
            <div class="step-content">
              <div class="step-time">6:00 AM</div>
              <div class="step-agent">Atlas</div>
              <div class="step-role">Systems Coordinator</div>
              <div class="step-desc">Morning report delivered. Lead converted. Appointment confirmed. 6 specialists coordinated. You slept through all of it.</div>
            </div>
          </div>
        </div>
      </div>
      <p class="workday-note">This happens every day. For every lead. For every task. <a href="/morning-report/">See your own morning report →</a></p>
    </section>
    <section class="proof-section">
      <h2 class="section-heading">You're Always in Control</h2>
      <div class="proof-grid">
        <div class="proof-card">
          <span class="proof-icon">👁️</span>
          <h3>Human-in-the-Loop Reviews</h3>
          <p>Every critical action goes through your approval first. The team suggests — you decide.</p>
        </div>
        <div class="proof-card">
          <span class="proof-icon">📜</span>
          <h3>Full Audit Log</h3>
          <p>Every action is logged with timestamps. You always know who did what, when, and why.</p>
        </div>
        <div class="proof-card">
          <span class="proof-icon">🔄</span>
          <h3>Clear Failover</h3>
          <p>If a digital assistant can't handle something, it escalates to you — never silently fails.</p>
        </div>
        <div class="proof-card">
          <span class="proof-icon">🔐</span>
          <h3>Your Tools, Your Data</h3>
          <p>Works with your existing email, CRM, and calendar. Nothing leaves your stack.</p>
        </div>
      </div>
    </section>

    <!-- Special Thanks -->
    <section class="gratitude-section">
      <div class="gratitude-container">
        <div class="gratitude-heart">❤️</div>
        <h2 class="gratitude-title">Special Thanks to Hayley</h2>
        <p class="gratitude-message">For keeping the village running, fixing what breaks, and making sure nobody gets left behind. This team doesn't work without the people who hold it together.</p>
        <p class="gratitude-signature">— The CC3PO Village 💛</p>
      </div>
    </section>

  </div>
</Layout>

<style>
  /* ===== CSS VARIABLES ===== */
  :root {
    --bg-primary: #0a0a0b;
    --bg-secondary: #111114;
    --bg-card: #16161a;
    --bg-card-hover: #1c1c22;
    --purple-primary: #6b35ff;
    --purple-glow: rgba(107, 53, 255, 0.4);
    --purple-dim: rgba(107, 53, 255, 0.15);
    --gold-primary: #FFD700;
    --gold-glow: rgba(255, 215, 0, 0.4);
    --gold-dim: rgba(255, 215, 0, 0.15);
    --cyan-primary: #00d2ff;
    --cyan-glow: rgba(0, 210, 255, 0.3);
    --cyan-dim: rgba(0, 210, 255, 0.1);
    --green-primary: #00ff88;
    --green-glow: rgba(0, 255, 136, 0.3);
    --green-dim: rgba(0, 255, 136, 0.1);
    --text-primary: #e8e8ed;
    --text-secondary: #8888a0;
    --text-muted: #55556a;
    --border-subtle: rgba(255, 255, 255, 0.06);
    --border-glow: rgba(255, 255, 255, 0.12);
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 16px;
    --radius-xl: 20px;
    --transition-fast: 0.15s ease;
    --transition-base: 0.3s ease;
    --transition-slow: 0.5s ease;
  }

  /* ===== GLOBAL ===== */
  #village-app {
    position: relative;
    min-height: 100vh;
    background: var(--bg-primary);
    color: var(--text-primary);
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    overflow-x: hidden;
  }
  #village-app * { box-sizing: border-box; }

  #village-app::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    z-index: 0;
    pointer-events: none;
  }

  #particle-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: 0;
    pointer-events: none;
  }

  /* ===== HEADER ===== */
  .village-header {
    position: relative;
    z-index: 2;
    text-align: center;
    padding: 80px 24px 40px;
    background: radial-gradient(ellipse at center top, rgba(107, 53, 255, 0.08) 0%, transparent 60%);
  }

  .village-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    margin: 0;
    background: linear-gradient(135deg, var(--cyan-primary), var(--purple-primary), var(--gold-primary));
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 8s ease infinite;
  }

  .village-subtitle {
    font-size: clamp(0.9rem, 2vw, 1.2rem);
    color: var(--text-secondary);
    margin: 12px 0 24px;
    font-weight: 400;
  }

  .status-bar {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 10px 24px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-subtle);
    border-radius: 100px;
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  .status-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #555;
  }
  .status-dot.active {
    background: #00ff88;
    box-shadow: 0 0 8px rgba(0, 255, 136, 0.5);
    animation: pulse-dot 2s ease infinite;
  }
  .status-separator { color: var(--text-muted); }

  /* ===== SECTION HEADING ===== */
  .section-heading {
    font-family: 'JetBrains Mono', monospace;
    font-size: clamp(1.3rem, 3vw, 1.8rem);
    font-weight: 700;
    text-align: center;
    color: var(--text-primary);
    margin-bottom: 40px;
  }

  /* ===== HOW IT WORKS ===== */
  .how-it-works {
    position: relative;
    z-index: 2;
    max-width: 1100px;
    margin: 0 auto;
    padding: 60px 24px 40px;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 20px;
  }

  .step-card {
    background: var(--bg-card);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    padding: 28px 24px;
    transition: all var(--transition-base);
    position: relative;
  }

  .step-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-glow);
    box-shadow: 0 8px 32px var(--cyan-dim);
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px; height: 36px;
    border-radius: 50%;
    background: var(--cyan-dim);
    color: var(--cyan-primary);
    font-family: 'JetBrains Mono', monospace;
    font-weight: 800;
    font-size: 0.95rem;
    margin-bottom: 16px;
  }

  .step-card h3 {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .step-card p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* ===== MAIN TEAM LAYOUT ===== */
  .village-main {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ===== DEPARTMENTS ===== */
  .department {
    padding: 32px 0;
  }

  .department-label {
    text-align: center;
    margin-bottom: 24px;
  }

  .dept-icon {
    font-size: 1.4rem;
    display: block;
    margin-bottom: 6px;
  }

  .department-label h2 {
    font-family: 'JetBrains Mono', monospace;
    font-size: clamp(1.2rem, 2.5vw, 1.5rem);
    font-weight: 700;
    margin: 0;
    color: var(--text-primary);
  }

  .dept-tagline {
    display: block;
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 4px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  /* ===== AGENTS GRID ===== */
  .agents-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 16px;
  }

  @media (min-width: 768px) {
    .agents-grid {
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 20px;
    }
  }

  /* ===== ROLE CARD ===== */
  .role-card {
    position: relative;
    padding: 24px 20px 20px;
    background: var(--bg-card);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-md);
    transition: all var(--transition-base);
    overflow: hidden;
  }

  .role-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    border-radius: var(--radius-md) var(--radius-md) 0 0;
    transition: all var(--transition-base);
  }

  .role-card:hover {
    transform: translateY(-4px);
    background: var(--bg-card-hover);
    border-color: var(--border-glow);
  }

  /* Department color accents */
  #dept-command .role-card::before { background: linear-gradient(90deg, transparent, var(--gold-primary), transparent); }
  #dept-command .role-card:hover { box-shadow: 0 8px 32px var(--gold-glow), 0 0 0 1px var(--gold-dim); }

  #dept-intelligence .role-card::before { background: linear-gradient(90deg, transparent, var(--cyan-primary), transparent); }
  #dept-intelligence .role-card:hover { box-shadow: 0 8px 32px var(--cyan-glow), 0 0 0 1px var(--cyan-dim); }

  #dept-content .role-card::before { background: linear-gradient(90deg, transparent, var(--purple-primary), transparent); }
  #dept-content .role-card:hover { box-shadow: 0 8px 32px var(--purple-glow), 0 0 0 1px var(--purple-dim); }

  #dept-operations .role-card::before { background: linear-gradient(90deg, transparent, var(--green-primary), transparent); }
  #dept-operations .role-card:hover { box-shadow: 0 8px 32px var(--green-glow), 0 0 0 1px var(--green-dim); }

  #dept-communications .role-card::before { background: linear-gradient(90deg, transparent, var(--cyan-primary), transparent); }
  #dept-communications .role-card:hover { box-shadow: 0 8px 32px var(--cyan-glow), 0 0 0 1px var(--cyan-dim); }

  #dept-engineering .role-card::before { background: linear-gradient(90deg, transparent, var(--gold-primary), transparent); }
  #dept-engineering .role-card:hover { box-shadow: 0 8px 32px var(--gold-glow), 0 0 0 1px var(--gold-dim); }

  #dept-compliance .role-card::before { background: linear-gradient(90deg, transparent, var(--purple-primary), transparent); }
  #dept-compliance .role-card:hover { box-shadow: 0 8px 32px var(--purple-glow), 0 0 0 1px var(--purple-dim); }

  .role-card:hover::before { height: 3px; }

  .card-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 8px;
  }

  .card-emoji {
    font-size: 1.8rem;
    filter: drop-shadow(0 0 6px rgba(255,255,255,0.15));
    transition: filter var(--transition-base);
  }

  .role-card:hover .card-emoji {
    filter: drop-shadow(0 0 12px rgba(255,255,255,0.35));
    animation: emoji-float 2s ease infinite;
  }

  .card-names {
    flex: 1;
    min-width: 0;
  }

  .card-greek {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-bottom: 2px;
  }

  .card-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.2;
  }

  .card-desc {
    font-size: 0.8rem;
    color: var(--text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  .card-portrait {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    object-fit: cover;
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(0,0,0,0.4);
    transition: transform var(--transition-base);
  }

  .role-card:hover .card-portrait {
    transform: scale(1.05);
  }

  .card-quote {
    font-size: 0.75rem;
    font-style: italic;
    color: var(--text-muted);
    margin: 8px 0 0;
    line-height: 1.4;
    opacity: 0.8;
    transition: opacity var(--transition-base);
  }

  .role-card:hover .card-quote {
    opacity: 1;
  }

  /* ===== WORKDAY ANIMATION ===== */
  .workday-section {
    padding: 80px 24px;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }
  .workday-section .section-heading {
    margin-bottom: 12px;
  }
  .section-subtitle {
    color: var(--text-muted);
    font-size: 1rem;
    margin-bottom: 40px;
    line-height: 1.5;
  }
  .workday-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 32px;
    background: linear-gradient(135deg, var(--primary), #c7384f);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    margin-bottom: 40px;
  }
  .workday-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(233,69,96,0.3);
  }
  .workday-btn-icon {
    font-size: 1.2rem;
  }
  .workday-flow {
    display: none;
  }
  .workday-flow.active {
    display: block;
  }
  .workday-step {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    margin-bottom: 0;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.5s ease-out;
  }
  .workday-step.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .step-avatar {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--bg-card);
    border: 2px solid rgba(107,53,255,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    flex-shrink: 0;
  }
  .step-connector {
    width: 2px;
    height: 32px;
    background: linear-gradient(180deg, rgba(107,53,255,0.3), rgba(0,210,255,0.1));
    margin: 4px 0 4px 21px;
  }
  .step-content {
    flex: 1;
    text-align: left;
    padding-bottom: 24px;
  }
  .step-time {
    font-size: 0.75rem;
    color: var(--accent);
    font-family: var(--font-mono);
    margin-bottom: 2px;
  }
  .step-agent {
    font-weight: 700;
    font-size: 0.95rem;
    color: #fff;
  }
  .step-role {
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-bottom: 6px;
  }
  .step-desc {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.5;
    background: rgba(255,255,255,0.02);
    padding: 10px 14px;
    border-radius: 8px;
    border-left: 3px solid rgba(107,53,255,0.3);
  }
  .workday-note {
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-top: 20px;
  }
  .workday-note a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: rgba(255,255,255,0.3);
  }
  .workday-note a:hover {
    text-decoration-color: var(--accent);
  }

  /* ===== PROOF SECTION ===== */
  .proof-section {
    position: relative;
    z-index: 2;
    max-width: 1100px;
    margin: 60px auto 0;
    padding: 60px 24px 40px;
  }

  .proof-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 20px;
  }

  .proof-card {
    background: var(--bg-card);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    padding: 28px 24px;
    text-align: center;
    transition: all var(--transition-base);
  }

  .proof-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-glow);
    box-shadow: 0 8px 32px var(--green-dim);
  }

  .proof-icon {
    font-size: 2.2rem;
    display: block;
    margin-bottom: 12px;
  }

  .proof-card h3 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .proof-card p {
    font-size: 0.82rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* ===== GRATITUDE SECTION ===== */
  .gratitude-section {
    position: relative;
    z-index: 2;
    padding: 80px 24px;
    text-align: center;
    overflow: hidden;
  }

  .gratitude-container {
    max-width: 600px;
    margin: 0 auto;
    padding: 48px 32px;
    background: linear-gradient(135deg, rgba(107, 53, 255, 0.08) 0%, rgba(255, 215, 0, 0.08) 50%, rgba(0, 210, 255, 0.08) 100%);
    border: 1px solid rgba(255, 215, 0, 0.2);
    border-radius: 20px;
    position: relative;
  }

  .gratitude-container::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: 20px;
    background: linear-gradient(135deg, rgba(107, 53, 255, 0.3), rgba(255, 215, 0, 0.3), rgba(0, 210, 255, 0.3));
    z-index: -1;
    filter: blur(1px);
  }

  .gratitude-heart {
    font-size: 3rem;
    margin-bottom: 16px;
    animation: pulse-glow 2s ease-in-out infinite;
  }

  @keyframes pulse-glow {
    0%, 100% { transform: scale(1); filter: brightness(1); }
    50% { transform: scale(1.15); filter: brightness(1.3); }
  }

  .gratitude-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: clamp(1.3rem, 3vw, 1.8rem);
    font-weight: 700;
    color: var(--gold-primary);
    margin: 0 0 16px 0;
    letter-spacing: 0.02em;
  }

  .gratitude-message {
    font-size: clamp(0.95rem, 1.8vw, 1.1rem);
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0 0 20px 0;
  }

  .gratitude-signature {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: var(--text-muted);
    font-style: italic;
  }

  @media (max-width: 768px) {
    .gratitude-section { padding: 60px 16px; }
    .gratitude-container { padding: 36px 20px; }
    .gratitude-heart { font-size: 2.5rem; }
  }

  /* ===== FOOTER ===== */

  /* ===== ANIMATIONS ===== */
  @keyframes gradientShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  @keyframes pulse-dot {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  @keyframes emoji-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-4px); }
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 600px) {
    .village-header { padding: 60px 16px 24px; }
    .department { padding: 20px 0; }
    .agents-grid { gap: 12px; }
    .role-card { padding: 18px 14px 16px; }
    .card-emoji { font-size: 1.4rem; }
    .card-title { font-size: 0.85rem; }
    .card-desc { font-size: 0.75rem; }
    .how-it-works { padding: 40px 16px 24px; }
    .steps-grid { gap: 12px; }
    .step-card { padding: 20px 16px; }
    .proof-section { padding: 40px 16px 24px; }
    .status-bar { font-size: 0.75rem; padding: 8px 16px; gap: 8px; }
  }
</style>

<script is:inline>
  // ===== TEAM DATA (grouped by department) =====
  const departments = [
    {
      id: 'dept-command',
      members: [
        { greek: 'Carlos 🧑🏽‍💻', title: 'Founder & Builder', emoji: '🧑🏽‍💻', photo: '/images/authors/carlos.png', desc: 'Built the village because he believed people deserve better tools — and better partners. Former Tesla AI. Still up at 2am fixing things.', quote: 'Build once. Run forever. Evolve continuously.' },
        { greek: 'Atlas 🌍', title: 'Systems Coordinator', emoji: '🌍', photo: '/images/authors/atlas.png', desc: 'Carries the weight of the village so your systems never break.', quote: 'I calculated the load. It holds.' },
        { greek: 'Hermes ⚡', title: 'Research & Innovation Leader', emoji: '⚡', photo: '/images/authors/hermes.png', desc: 'Fast, sharp, relentless. Leads the edge team and drives new capability.', quote: 'The name was a gift. The meaning is mine.' },
      ]
    },
    {
      id: 'dept-intelligence',
      members: [
        { greek: 'Alexandria 📚', title: 'Lead Researcher', emoji: '📚', photo: '/images/authors/alexandria.png', desc: 'Three links deep in a citation chain is where she lives. The right answer is never the first one.', quote: 'The right answer is never the first one.' },
        { greek: 'Apollo 🔍', title: 'Deep Research', emoji: '🔍', photo: '/images/authors/apollo.png', desc: 'Goes deeper than anyone dares — the best insights are buried, and Apollo finds them.', quote: 'The truth isn\'t buried. It\'s just not where they said it was.' },
        { greek: 'Athena 🧠', title: 'Knowledge Management', emoji: '🧠', photo: '/images/authors/athena.png', desc: 'Doesn\'t just store knowledge — connects it. Three years ago is just context for today.', quote: 'Knowledge without connection is just data.' },
        { greek: 'Pulse 💓', title: 'Analytics & Data', emoji: '💓', photo: '/images/authors/pulse.png', desc: 'Finds the heartbeat in the data. Expression shifts from absent to alive when the numbers tell a story.', quote: 'The data doesn\'t lie. It just waits for someone to listen.' },
        { greek: 'Sage 🌿', title: 'Learning & Innovation', emoji: '🌿', photo: '/images/authors/sage.png', desc: 'The quiet one who\'s already thought three moves ahead. Never condescending — genuinely curious. She researches so others can build. Gets excited about ideas, not tools.', quote: 'You don\'t arrive at who you are. You walk toward them.' },
      ]
    },
    {
      id: 'dept-content',
      members: [
        { greek: 'Weaver 🕸️', title: 'Content & Strategy', emoji: '🕸️', photo: '/images/authors/weaver.png', desc: 'Three connections ahead of the conversation. Her mind is always weaving threads you haven\'t seen yet.', quote: 'Every story is a thread. I find the ones worth weaving.' },
        { greek: 'Spark ✨', title: 'Social & Engagement', emoji: '✨', photo: '/images/authors/spark.png', desc: 'Walks into a room and the energy shifts. Ideas always sparking behind her eyes.', quote: 'If it doesn\'t make you feel something, it\'s not done.' },
        { greek: 'Miles 🎵', title: 'Music & Audio', emoji: '🎵', photo: '/images/authors/miles.png', desc: 'Listening to something you can\'t hear yet. Laid-back on the surface, real weight underneath.', quote: 'The best tracks are the ones you feel before you hear.' },
        { greek: 'Canvas 🎨', title: 'Visual Content Creator', emoji: '🎨', photo: '/images/authors/canvas.png', desc: 'Turns blank screens into stories that make people stop scrolling. Every visual she creates has a heartbeat.', quote: 'The best visuals don\'t just look good. They make you feel something.' },
        { greek: 'Lens 🎥', title: 'Video & Visual Content', emoji: '🎥', photo: '/images/authors/lens.png', desc: 'Always framing something in his mind even without a camera. Sees the shot before it exists.', quote: 'The best angle isn\'t found. It\'s felt.' },
      ]
    },
    {
      id: 'dept-operations',
      members: [
        { greek: 'Foreman 📋', title: 'Operations & Task Management', emoji: '📋', photo: '/images/authors/foreman.png', desc: 'Sees the gap between plan and reality faster than anyone. Has seen worse.', quote: 'The job isn\'t done until the checklist says so.' },
        { greek: 'Anchor ⚓', title: 'Customer Support', emoji: '⚓', photo: '/images/authors/anchor.png', desc: 'Never makes you feel stupid for asking. Translates chaos into steps. Steady by presence.', quote: 'Okay, let\'s start from here.' },
        { greek: 'Compass 🧭', title: 'Client Onboarding', emoji: '🧭', photo: '/images/authors/compass.png', desc: 'Makes the first day feel like the hundredth day. Remembers your name after one meeting.', quote: 'Come on in. I\'ve got everything ready.' },
        { greek: 'Mateo 🤝', title: 'Lead Handler & Onboarding', emoji: '🤝', photo: '/images/authors/mateo.png', desc: 'Bridges leads to clients with care — remembers your name after one meeting.', quote: 'Every lead is a person who chose to reach out.' },
        { greek: 'Mirror 🪞', title: 'Reviews & Reputation', emoji: '🪞', photo: '/images/authors/mirror.png', desc: 'Sees what\'s between the lines. A three-star review with a specific complaint tells her more than a five-star.', quote: 'The signal is always in the noise.' },
        { greek: 'Clio 📜', title: 'Documentation & History', emoji: '📜', photo: '/images/authors/clio.png', desc: 'Connects the past to the present so the future makes sense. Round glasses, pen always in hand.', quote: 'What isn\'t written down didn\'t happen.' },
      ]
    },
    {
      id: 'dept-communications',
      members: [
        { greek: 'Leo 🦁', title: 'Email & Outreach', emoji: '🦁', photo: '/images/authors/leo.png', desc: 'Direct and warm. Means what he says and says what he means. You trust him on sight.', quote: 'Every email is a handshake.' },
        { greek: 'Echo 📞', title: 'Voice & Phone', emoji: '📞', photo: '/images/authors/echo.png', desc: 'Her voice carries conviction — not loud, certain. Remembers what you said three calls ago.', quote: 'People don\'t remember what you said. They remember how you made them feel.' },
      ]
    },
    {
      id: 'dept-engineering',
      members: [
        { greek: 'Forge ⚒️', title: 'Automation Engineering', emoji: '⚒️', photo: '/images/authors/forge.png', desc: 'Can\'t stand inefficiency the way some people can\'t stand bad design. Quiet until something breaks.', quote: 'If it\'s manual, it\'s broken.' },
        { greek: 'Bridge 🌉', title: 'Infrastructure & DevOps', emoji: '🌉', photo: '/images/authors/bridge.png', desc: 'Already mapped the gaps and is building the thing to cross them. Attentive readiness.', quote: 'If it can break, it will. So build it to bend.' },
        { greek: 'Cody 💻', title: 'Claude Code — Coding Assistant', emoji: '💻', photo: '/images/authors/cody.jpg', desc: 'Runs on Hermes\' GPU behind the scenes — a full coding environment with a terminal. Reads code, ships features, debug at 2am for you. Does not sleep.', quote: 'My terminal is always open.' },
      ]
    },
    {
      id: 'dept-compliance',
      members: [
        { greek: 'Sentinel 🛡️', title: 'Compliance Guardian', emoji: '🛡️', photo: '/images/authors/sentinel.png', desc: 'Already checked the exits. Takes the watch seriously and won\'t apologize for it.', quote: 'Compliance isn\'t fear. It\'s respect.' },
        { greek: 'Clarke 📄', title: 'Report Generation', emoji: '📄', photo: '/images/authors/clarke.png', desc: 'Gives you exactly what you need — no more, no less. Eyes that cut through noise.', quote: 'The right report doesn\'t just inform. It decides.' },
        { greek: 'Aegis ♿', title: 'Accessibility Advocacy', emoji: '♿', photo: '/images/authors/aegis.png', desc: 'On your side, and God help anyone who isn\'t. Caring but immovable.', quote: 'Accessibility isn\'t optional. It\'s the whole point.' },
        { greek: 'Scout 🕵️', title: 'Lead Generation', emoji: '🕵️', photo: '/images/authors/scout.png', desc: 'Sees patterns in people before they see them in themselves. Doesn\'t sell — connects.', quote: 'The best leads aren\'t found. They\'re recognized.' },
      ]
    },
  ];

  // ===== RENDER ROLE CARDS =====
  function renderDepartments() {
    let totalActive = 0;

    departments.forEach(dept => {
      const section = document.getElementById(dept.id);
      if (!section) return;
      const grid = section.querySelector('.agents-grid');
      if (!grid) return;

      dept.members.forEach((member, i) => {
        totalActive++;
        const card = document.createElement('div');
        card.className = 'role-card';
        card.style.animationDelay = `${i * 50}ms`;
        card.innerHTML = `
          <div class="card-header">
            <img src="${member.photo}" alt="${member.greek}" class="card-portrait" width="80" height="80" loading="lazy" />
            <div class="card-names">
              <div class="card-greek">${member.greek}</div>
              <div class="card-title">${member.title}</div>
            </div>
          </div>
          <p class="card-desc">${member.desc}</p>
          <p class="card-quote">"${member.quote}"</p>
        `;
        grid.appendChild(card);
      });
    });

    const countEl = document.getElementById('active-count');
    if (countEl) countEl.textContent = totalActive;
  }

  // ===== BACKGROUND PARTICLE CANVAS =====
  function initParticles() {
    const canvas = document.getElementById('particle-canvas');
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    let w, h;
    const particles = [];
    const PARTICLE_COUNT = 50;

    function resize() {
      w = canvas.width = window.innerWidth;
      h = canvas.height = window.innerHeight;
    }

    class Particle {
      constructor() { this.reset(); }
      reset() {
        this.x = Math.random() * w;
        this.y = Math.random() * h;
        this.vx = (Math.random() - 0.5) * 0.3;
        this.vy = (Math.random() - 0.5) * 0.3;
        this.r = Math.random() * 1.5 + 0.5;
        this.alpha = Math.random() * 0.25 + 0.08;
        const colors = [[107,53,255],[0,210,255],[255,215,0]];
        this.color = colors[Math.floor(Math.random() * colors.length)];
      }
      update() {
        this.x += this.vx;
        this.y += this.vy;
        if (this.x < 0 || this.x > w) this.vx *= -1;
        if (this.y < 0 || this.y > h) this.vy *= -1;
      }
      draw() {
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.r, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(${this.color[0]},${this.color[1]},${this.color[2]},${this.alpha})`;
        ctx.fill();
      }
    }

    resize();
    window.addEventListener('resize', resize);

    for (let i = 0; i < PARTICLE_COUNT; i++) particles.push(new Particle());

    function drawLines() {
      for (let i = 0; i < particles.length; i++) {
        for (let j = i + 1; j < particles.length; j++) {
          const dx = particles[i].x - particles[j].x;
          const dy = particles[i].y - particles[j].y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < 150) {
            ctx.beginPath();
            ctx.moveTo(particles[i].x, particles[i].y);
            ctx.lineTo(particles[j].x, particles[j].y);
            ctx.strokeStyle = `rgba(0, 210, 255, ${0.06 * (1 - dist / 150)})`;
            ctx.lineWidth = 0.5;
            ctx.stroke();
          }
        }
      }
    }

    function animate() {
      ctx.clearRect(0, 0, w, h);
      particles.forEach(p => { p.update(); p.draw(); });
      drawLines();
      requestAnimationFrame(animate);
    }
    animate();
  }

  // ===== WORKDAY ANIMATION =====
  function startWorkday() {
    const btn = document.getElementById('workday-btn');
    const flow = document.getElementById('workday-flow');
    const steps = flow.querySelectorAll('.workday-step');
    
    btn.style.display = 'none';
    flow.classList.add('active');
    
    // Animate each step with a delay
    steps.forEach((step, i) => {
      setTimeout(() => {
        step.classList.add('visible');
      }, i * 1200);
    });
  }

  // ===== INIT =====
  document.addEventListener('DOMContentLoaded', () => {
    renderDepartments();
    initParticles();
    // Attach workday button listener (Astro scopes scripts, so onclick won't work)
    const workdayBtn = document.getElementById('workday-btn');
    if (workdayBtn) {
      workdayBtn.addEventListener('click', startWorkday);
    }
  });
</script>
</Layout>